Get-ChildItem -Path . -Recurse -File | Where-Object { $_.Length -ge 40MB } | ForEach-Object {
    Write-Host "Removing: $($_.FullName) ($([math]::Round($_.Length / 1MB, 2)) MB)"
    Remove-Item $_.FullName -Force
}